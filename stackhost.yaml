user:
  name: Mrking671
  github: Mrking671

python_version: "3.11"

commands:
  build:
    - "pip install --no-cache-dir -r requirements.txt"
  start:
    - "user:
  name: Mrking671
  github: Mrking671

python_version: "3.11"

commands:
  build:
    - "pip install --no-cache-dir -r requirements.txt"
  start:
    - "python bot.py"
